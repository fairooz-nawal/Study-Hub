import React from 'react';
import Header from './header and footer/Header';

const Root = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default Root;